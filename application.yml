spring:
  application:
    name: greeting-service
greeting-service:
  greeting: "Hello"
  default-value: "World"
  
# Spring Boot Actuator  
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
